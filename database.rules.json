{
  "rules": {
    ".read": false,
    ".write": false,
    "order_items": {
      "$stationId": {
        ".read": "auth != null && auth.token.stationId === $stationId",
        ".write": "auth != null && auth.token.stationId === $stationId"
      }
    }
  }
}
